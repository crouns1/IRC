#ifndef PARSER_HPP
#define PARSER_HPP

#include <vector.h>

struct Command {
  std::string name;
  std::vector<std::string> params;
  bool hasTrailing;
  std::string;
};

class CommandHandler {
private:
  
}

#endif
